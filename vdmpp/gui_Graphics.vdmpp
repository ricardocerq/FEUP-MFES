class gui_Graphics

	operations
		public init : () ==> ()
			init() == 
				is not yet specified
		;
		
		public sendData: seq of ? ==> ()
			sendData(list) == 
				is not yet specified
		;
		
		
end gui_Graphics
